<h1><%= @patient.name %></h1>

<h3>Appointments</h3>

<ul>
    <% @patient.appointments.each do |apt| %>
    <li>
        <%= apt.appointment_datetime.strftime("%B %e, %Y at %H:%M") %><br>
        <%= link_to apt.doctor.name, doctor_path(apt.doctor) %>    </li>
    <% end %>
</ul>